%h1
  = @item.name
= link_to "edit", edit_item_path(@item)
%h2
  Specifications:
- @item.option_groups.each do |group|
  %h3
    #{group.name}:
  - group.options.each do |option|
    %p
      #{option.name}:
      = option.value